[project]
name = "etl_pipeline"
version = "0.1.0"
description = "CLEWS to OG-Core ETL Pipeline"
dependencies = [
    "pandas",
    "pyyaml",
    "pydantic",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
